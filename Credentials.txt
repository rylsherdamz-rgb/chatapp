

https://medium.com/@aysunitai/building-a-simple-video-chat-app-with-react-and-peerjs-5650cf89569d

visit this site tommorow to add peerjs to maket his work and even allow multiple account to have videocall



// Caller
socketClient.emit("call-user", { to: remotePeerId, type: "Video" })

// Server forwards:
socket.to(remotePeerId).emit("incoming-call", { from: callerId, type })

// Receiver listens:
socketClient.on("incoming-call", ({ from, type }) => {
  setRemotePeerId(from)
  setCallType(type)
  setCallOpen(true)
})


take this into considetaion that is is what i need to do is to add a caller and an on listen avent for this
so i can have th facebook messenger chat functionality
